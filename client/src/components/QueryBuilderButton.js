import { Button } from "@mui/material";

export default function QueryBuilderButton () {
    return (
        <Button  sx={{ color: 'black', backgroundColor: 'yellow', minWidth: 'max-content', whitespace: 'nowrap'}}>
            Query Builder
        </Button>
    );
}